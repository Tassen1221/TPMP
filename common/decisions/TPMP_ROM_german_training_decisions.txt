TPMP_ROM_deployment_decisions = {

	TPMP_ROM_wave_deployment_mission = {

		icon = GFX_decision_generic_political_discourse

		allowed = {
			always = no
		}

		available = {
			hidden_trigger = { always = no }
		}

		days_mission_timeout = 90
		is_good = yes
		fire_only_once = yes

		complete_effect = {
		}

		timeout_effect = {

			random_owned_controlled_state = {
				limit = { ROOT = { has_full_control_of_state = PREV } }
				create_unit = {
					division = "name = \"Infanterie de Linie\" division_template = \"TPMP_ROM_german_trained_infantry\" start_experience_factor = 0.3"
					owner = ROOT
					count = 8
				}
			}

			add_to_variable = { var = TPMP_ROM_wave_deployments_remaining value = -1 }

			if = {
				limit = {
					check_variable = { var = TPMP_ROM_wave_deployments_remaining value = 0 compare = greater_than }
				}
				hidden_effect = {
					activate_mission = TPMP_ROM_wave_deployment_mission
				}
			}
		}
	}

	TPMP_ROM_use_custom_equipment_decision = {

		icon = GFX_decision_generic_industry

		allowed = {
			original_tag = ROM
		}

		available = {
			always = yes
		}

		visible = {
			NOT = { has_country_flag = TPMP_ROM_use_custom_equipment }
		}

		complete_effect = {
			set_country_flag = TPMP_ROM_use_custom_equipment
		}

		ai_will_do = {
			factor = 0
		}
	}

	TPMP_ROM_use_default_equipment_decision = {

		icon = GFX_decision_generic_industry

		allowed = {
			original_tag = ROM
		}

		available = {
			always = yes
		}

		visible = {
			has_country_flag = TPMP_ROM_use_custom_equipment
		}

		complete_effect = {
			clr_country_flag = TPMP_ROM_use_custom_equipment
		}

		ai_will_do = {
			factor = 0
		}
	}
}
