# TAOG Army HQ templates intentionally disabled for TPMP.
# The mod does not implement the TAOG HQ subunits (hq_motorized, hq_infantry, hq_support_company, etc.),
# so the vanilla templates would reference invalid subunits. This empty override prevents those errors.
